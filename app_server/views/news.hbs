<!DOCTYPE html>
<!-- Website template by freewebsitetemplates.com -->
<html>
    <head>
        <meta charset="UTF-8">
        <title>{{title}}</title>
        <link rel="stylesheet" href="css/style.css" type="text/css">
    </head>
    <body>
        <div id="background">
            <div id="page">
                {{> header}}
                <div id="contents">
                    <div class="box">
                        <div>
                            <div id="news" class="body">
                                <div class="sidebar">
                                    <h3>Latest News</h3>
                                    <ul>
                                        {{#each latestNews}}
                                        <li>
                                            <a href="{{this.link}}">{{this.title}}</a>
                                        </li>
                                        {{/each}}
                                    </ul>
                                    <h3>Vacation Tips</h3>
                                    <ul>
                                        {{#each vacationTips}}
                                        <li>
                                            <a href="{{this.link}}">{{this.title}}</a>
                                        </li>
                                        {{/each}}
                                    </ul>
                                </div>
                                <div>
                                    <h1>News</h1>
                                    <img src="images/{{featuredNews.image}}" alt="{{featuredNews.alt}}">
                                    <h2>{{featuredNews.title}}</h2>
                                    <span class="time">{{featuredNews.publishDate}}<br> by: {{featuredNews.author}}</span>
                                    {{{featuredNews.content}}}
                                    </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {{> footer}}
        </div>
    </body>
</html>